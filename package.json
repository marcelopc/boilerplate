{
  "name": "boilerplate",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && ts-node-dev -r tsconfig-paths/register --respawn --transpile-only  --no-notify --inspect -- ./src/index.ts",
    "build": "tsc -p tsconfig.build.json && tsc-alias"
  },
  "keywords": [],
  "author": "marcelopc",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0",
    "tsc-alias": "^1.8.8",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
